# Leave empty for local dev — the Vite proxy forwards /api to localhost:4000.
# In production set this to your deployed API base, e.g.
# VITE_API_URL=https://token-town-api.azurewebsites.net
VITE_API_URL=
