# Local Postgres connection. Change user/password/db to match your setup.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tokentown?schema=public"

# Port the API listens on (Azure App Service overrides this automatically).
PORT=4000

# Where the frontend is served from, for CORS. Vite's dev server default:
CORS_ORIGIN="http://localhost:5173"
